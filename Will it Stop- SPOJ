#include<bits/stdc++.h>
using namespace std;
char* isPowerOfTwo(long long int n)
{
  if (n == 0)
    return "NIE";
  while (n != 1)
  {
    if (n%2 != 0)
      return "NIE";
    n = n/2;
  }
  return "TAK";
}
int main()
{
    long long int n;
    cin>>n;
    cout<<isPowerOfTwo(n)<<endl;
}
